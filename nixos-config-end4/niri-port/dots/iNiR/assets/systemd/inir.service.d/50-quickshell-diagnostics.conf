# Optional copy to ~/.config/systemd/user/inir.service.d/ when debugging Quickshell
# segfaults on NixOS + NVIDIA. See repo README (Quickshell / iNiR section).
[Service]
Environment=LIBGL_ALWAYS_SOFTWARE=1
LimitCORE=infinity
